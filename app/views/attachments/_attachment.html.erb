<% if attachment.image? %>
  <img style="max-width: 100%;" src="<%= attachment.media.expiring_url(10) %>" />
<% else %>
  <a href="<%= attachment.media.expiring_url(10) %>">View Attachment</a>
<% end %>
