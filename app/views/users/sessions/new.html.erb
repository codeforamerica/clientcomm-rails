<div class="slab">
  <div class="grid">
    <div class="grid__item width-two-thirds shift-one-sixth">
      <div class="form-card form-card--compact">

        <div class="form-card__header">
          <div class="toolbar">
            <div class="toolbar__left">
              <h1 class="form-card__title">Log in</h1>
            </div>
          </div>
        </div>

        <div class="form-card__content">
        <%= form_for resource, as: resource_name, url: session_path(resource_name), builder: GcfFormBuilder do |f| %>
          <%= f.gcf_input_field :email, "Email", autofocus: true, classes: ['form-width--long'] %>
          <div class="form-group">
            <%= f.label :password, "Password", class: 'form-question' %>
            <%= f.password_field :password, autocomplete: "off", class: 'text-input form-width--long' %>
          </div>
          <div class="form-card__footer">
            <div class="toolbar">
              <div class="toolbar__left">
                <div class="toolbar__item">
                  <%= f.continue "Sign in" %>
                </div>
                <% if devise_mapping.rememberable? -%>
                <div class="toolbar__item nudge--med">
                  <p class="text--help">
                    <%= f.check_box :remember_me %>
                    <%= f.label "Remember me" %>
                  </p>
                </div>
                <% end -%>
              </div>
            </div>
          </div>
        <% end %>

        </div>
      </div>
    </div>
  </div>
</div>
