ClientComm

<%= "#{@client.first_name} #{@client.last_name}" %> sent you a text message on <%= @message.created_at.strftime("%-m/%d") %>, at <%= @message.created_at.strftime("%-I:%M%p") %>:

<% if @message.body.present? %>
"<%= @message.body %>"
<% end %>

<%= '[Image attached, view on ClientComm]' if @message.attachments.present? %>
<% rr = ReportingRelationship.find_by(client: @message.client, user: @message.user) %>
Respond on ClientComm: <%= reporting_relationship_url(rr) %>
