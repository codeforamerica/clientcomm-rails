<%= t('devise.mailer.invitation_instructions.someone_invited_you') %>

Set up your account: <%= accept_invitation_url(@resource, :invitation_token => @token) %>

<% if @resource.invitation_due_at %>
  <%= t("devise.mailer.invitation_instructions.accept_until", due_date: l(@resource.invitation_due_at, format: :'devise.mailer.invitation_instructions.accept_until_format')) %>

<% end %>
<%= t('devise.mailer.invitation_instructions.questions') %>
<%= t('devise.mailer.invitation_instructions.sign_off') %>
<%= t('devise.mailer.invitation_instructions.cfa') %>
